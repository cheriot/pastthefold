package news.pastthefold.auth

import utest._

object SecureRequestServiceTest extends TestSuite {
  override def tests = Tests {
    "embedAuth - create" - { }

    "checkAuth - read" - { }

    "embed and check with same instance" - { }

    "embed and check with different instances" - { }
  }
}
